/**
 * This namespace contains functions for converting data
 * from your domain objects into the relation structure
 * used by your GraphQL endpoint.
 */
export const toExampleGraph = {
  item(item) { return item; },
};
