/**
 * This namespace contains functions for converting data
 * from the relational structure of the GraphQL endpoint
 * into domain objects for your application.
 */
export const fromExampleGraph = {
  item(item) { return item; },
};
